import {config} from 'dotenv';

config()
export const reservamos_url = process.env.RESERVAMOS_URL;
export const weather_base_url = process.env.WEATHER_BASE_URL;
export const weather_key = process.env.WEATHER_KEY